[pytest]
testpaths = tests
pythonpath = backend
asyncio_mode = auto
markers =
    unit: Test unitari — isolati con mock, nessuna dipendenza esterna
    integration: Test di integrazione — controller + service con TestClient
    system: Test di sistema — flussi end-to-end multi-componente
    asyncio: Test asincroni